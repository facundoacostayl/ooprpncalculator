using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Runtime.ConstrainedExecution;
using System.Threading;

namespace UnitTestProject2
{

    #region

    /*
     
     * Un banco, ofrece a sus clientes la posibilidad de ser titulares de una cuenta bancaria. 
     * El titular de la cuenta debe tener nombre y apellido, y ser mayor de 18 años
     * La cuenta bancaria puede ser de tipo caja de ahorro o de tipo cuenta corriente.
     * La diferencia principal es que la cuenta corriente permite girar en descubierto hasta $5000 mientras que la caja de ahorro no es posible el giro en descubierto.
     * Las operaciones pueden ser tanto de depósito como de extracción.
     * Debe haber también un historial de los movimientos de cada cuenta, indicando fecha, hora, operación y monto.

     * De ser posible utilizar TDD/POO
     
    */

    #endregion

    [TestMethod]
        public void test()
        {
            
        }
    }
}
